Frontend files (HTML/CSS/JS) live under `www/` at the repo root. To work from the reorganized layout, update your tooling to use `frontend/www` or move the `www/` directory contents into `frontend/www`.

Current bridge entrypoint copied to `frontend/src/bridge/fincollect-bridge.js` for easy editing.