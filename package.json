{
  "name": "lot-field-collection",
  "version": "1.0.0",
  "description": "LOT Field Collection - Enterprise Field Collection Platform (Monorepo Root)",
  "private": true,
  "scripts": {
    "dev": "npx -y http-server ./frontend -p 3000 -c-1 --cors",
    "build:backend": "cd backend && tsc",
    "type-check": "cd backend && tsc --noEmit",
    "build:www": "node scripts/build-www.js",
    "start": "npm run dev"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "@types/node": "^20.14.0",
    "typescript": "^5.4.5"
  }
}
