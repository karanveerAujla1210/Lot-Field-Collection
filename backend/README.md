Backend moved from project root `src/backend` and `supabase`.

- Services: backend/src/services
- Config: backend/src/config
- Supabase edge functions and migrations: backend/supabase

Note: Update any relative imports if you run the backend from the new path.